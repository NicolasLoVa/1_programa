def tabla_1():
  n_tabla=input("ELIGE UNA TABLA (1-10): ")
  print("Has elegido la tabla del "+ n_tabla)
  for cont in range(1,11):
    print(n_tabla + " x " + str(cont) + " = " + str(int(n_tabla)*cont))
  

tabla_1()
